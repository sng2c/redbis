{
  "name": "redbis",
  "version": "0.1.0",
  "description": "RDBMS backend middleware proxy that provides a Redis protocol (RESP) interface",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "redbis": "./bin/redbis.js"
  },
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "tsc",
    "dev:memory": "ts-node src/index.ts",
    "dev:sqlite": "DATABASE_URL=sqlite://./data/redbis.db ts-node src/index.ts",
    "start:memory": "node dist/index.js",
    "start:sqlite": "DATABASE_URL=sqlite://./data/redbis.db node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "prepare": "npm run build",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "format": "prettier --write \"src/**/*.ts\"",
    "format:check": "prettier --check \"src/**/*.ts\""
  },
  "keywords": [
    "redis",
    "resp",
    "proxy",
    "middleware",
    "tcp-server",
    "rdbms",
    "sqlite",
    "database",
    "protocol"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^20.10.0",
    "eslint": "^10.4.0",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.8.3",
    "ts-node": "^10.9.0",
    "typescript": "^5.3.0",
    "typescript-eslint": "^8.59.4",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "better-sqlite3": "^12.10.0"
  }
}
